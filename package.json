{
  "name": "blog.serghei.pl",
  "description": "Personal blog",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sergeyklay/blog.serghei.pl.git"
  },
  "private": "true",
  "scripts": {
    "server": "hugo server --environment development --logLevel info --buildDrafts --buildFuture --ignoreCache --disableFastRender",
    "build": "hugo --environment production --gc --logLevel info --cleanDestinationDir --printUnusedTemplates --printPathWarnings --printMemoryUsage --printI18nWarnings --buildDrafts --buildExpired --buildFuture --forceSyncStatic"
  }
}
